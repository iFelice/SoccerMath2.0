"""Selettore Top Mix PRIMA del refactor: testo verbatim, non un riassunto.

Generato da ``audit/make_topmix_selector_fixture.py`` (``--check`` rigenera e
confronta) a partire da ``16d4e73:SoccerMath/app.py``. Usato da
``SoccerMath/test_topmix_selector_parity.py``, che esegue ``TESTO_FUNZIONE`` in un
namespace con gli stessi stub del percorso nuovo e confronta le righe prodotte:
stesso identico output = refactor neutro.

NON e' codice di produzione e non deve essere importato da ``app.py``. Se ti
serve cambiarlo, rigenera il fixture: modificarlo a mano distruggerebbe l'unica
prova che il confronto e' con il codice di prima.
"""
from __future__ import annotations

ORIGINE = '16d4e73'
BLOB = 'SoccerMath/app.py'
NOME = 'fetch_and_calc_top_mix'

# Corpo della funzione PRIMA del refactor (riga decoratore rimossa; il resto e'
# byte-identico al blob, vedi test_topmix_selector_parity.py::test_provenienza).
TESTO_FUNZIONE = 'def fetch_and_calc_top_mix():\n    """Top 10 del turno. Nessuna formula qui dentro e\' stata toccata.\n\n    Igienizzati solo i percorsi di degrado (audit/margini_migliorabili_topmix.md\n    §4): timeout sulla GET, fallback Elo marcato (prima `elo_prob = poisson_prob`\n    + `except: pass` rendevano vacuo il veto sul disaccordo e abbassavano la\n    soglia 1X2 a 0,55), coda di rate-limit solo FRA le leghe (l\'ultima non\n    aspetta piu\' nulla) e `rank` sulla riga, cosi\' il registro sa in che posizione\n    era finita la previsione.\n    """\n    all_preds, missing = [], []\n    leghe = list(LEAGUES_CONFIG.keys())\n    for i_lega, league in enumerate(leghe):\n        if i_lega:\n            # coda SOLO fra una lega e l\'altra (10 richieste/min sul piano free)\n            time.sleep(6.5)\n        engine = get_league_engine(league)\n        if not engine: missing.append(league); continue\n        team_stats, avg_h, avg_a, _ = engine\n        try:\n            r = requests.get(f"https://api.football-data.org/v4/competitions/{LEAGUE_CODE_MAP[league]}/matches", headers={\'X-Auth-Token\': API_KEY_DATA}, params={"status": "TIMED,SCHEDULED"}, timeout=15)\n            if r.status_code != 200: continue\n            # Fix bug Top Mix: si seleziona la prossima giornata realmente\n            # futura per data di gioco, non più il semplice min(matchday).\n            matches = select_next_matchday_matches(r.json().get(\'matches\', []))\n        except Exception as e:\n            logging.warning(f"Errore fetch Top Mix {league}: {e}")\n            continue\n        for match in matches:\n            h = match[\'homeTeam\'].get(\'shortName\') or match[\'homeTeam\'].get(\'name\', \'?\')\n            a = match[\'awayTeam\'].get(\'shortName\') or match[\'awayTeam\'].get(\'name\', \'?\')\n            h_s = team_stats.get(clean_name(h), {"att": 1.0, "def": 1.0})\n            a_s = team_stats.get(clean_name(a), {"att": 1.0, "def": 1.0})\n            \n            # Poisson a due teste: 1X2 da mercato normalizzato, O/U e GG da lambda base\n            m_poisson = get_full_poisson_two_heads(h_s, a_s, avg_h, avg_a)\n            mercati = {\n                f"Vittoria {h}": m_poisson["1"], "Pareggio": m_poisson["X"], f"Vittoria {a}": m_poisson["2"],\n                "Over 2.5": 1 - m_poisson["u25"], "Under 2.5": m_poisson["u25"],\n                "GG": m_poisson["gg"], "NG": 1 - m_poisson["gg"]\n            }\n            best_mkt = max(mercati, key=mercati.get)\n            poisson_prob = mercati[best_mkt]\n            \n            # Elo agreement (solo per 1X2)\n            elo_prob, elo_disponibile = poisson_prob, True\n            try:\n                elo_p = predict_elo_probs(h, a, league)\n                if best_mkt == f"Vittoria {h}":\n                    elo_prob = elo_p["1"]\n                elif best_mkt == f"Vittoria {a}":\n                    elo_prob = elo_p["2"]\n                elif best_mkt == "Pareggio":\n                    elo_prob = elo_p["X"]\n            except Exception as e:\n                # NON e\' piu\' un `except: pass` silenzioso: la riga viene\n                # marcata e giudicata come un totale (Poisson puro, soglia\n                # 0,60), perche\' senza Elo la confidence non e\' un consenso.\n                elo_disponibile = False\n                logging.warning(f"Elo non disponibile per {h} vs {a} ({league}): {e}")\n            \n            # Confidence = media tra Poisson ed Elo (se Elo è vicino, conferma; se lontano, penalizza)\n            # Per mercati O/U e GG dove Elo non esiste, usiamo solo Poisson ma richiediamo soglia più alta\n            if best_mkt in ["Over 2.5", "Under 2.5", "GG", "NG"] or not elo_disponibile:\n                confidence = poisson_prob\n                min_conf = 0.60\n            else:\n                # Per 1X2: media armonica pesata (stesso peso dell\'ensemble\n                # ELO_ENSEMBLE_W: la confidence coincide con la probabilita\'\n                # 1X2 ensemble usata ovunque)\n                confidence = ELO_ENSEMBLE_W * poisson_prob + (1 - ELO_ENSEMBLE_W) * elo_prob\n                min_conf = 0.55\n            \n            # Filtro qualità: confidence minima e nessun disaccordo estremo\n            if confidence >= min_conf and abs(poisson_prob - elo_prob) < 0.25:\n                all_preds.append({\n                    "league": league, "giornata": match[\'matchday\'],\n                    "home": h, "away": a, "match_id": match.get("id"),\n                    "utcDate": match[\'utcDate\'], "market": best_mkt,\n                    "mercato_standard": codice_mercato_selezionato(best_mkt, h, a),\n                    "prob": confidence, "prob_val": round(confidence * 100, 1),\n                    "poisson": round(poisson_prob * 100, 1),\n                    "elo": round(elo_prob * 100, 1),\n                    # False SOLO se predict_elo_probs ha fallito: UI e registro\n                    # devono poter distinguere "Elo d\'accordo" da "Elo assente".\n                    "elo_disponibile": elo_disponibile,\n                    "rank": None,\n                })\n    top_10 = sorted(all_preds, key=lambda x: x[\'prob\'], reverse=True)[:10]\n    for i, r in enumerate(top_10):\n        r["rank"] = i + 1\n    return top_10, missing\n'

# Righe non-commento del blocco di selezione, dedentate: devono comparire sia
# qui sia nella nuova funzione pura. E' il guard contro le riscritture.
# Chiavi del dizionario che il selettore produceva (ordine originale).
CHIAVI_RIGA = ['league', 'giornata', 'home', 'away', 'match_id', 'utcDate', 'market', 'mercato_standard', 'prob', 'prob_val', 'poisson', 'elo', 'elo_disponibile', 'rank']
RIGHE_CHIAVE = ['mercati = {', 'f"Vittoria {h}": m_poisson["1"], "Pareggio": m_poisson["X"], f"Vittoria {a}": m_poisson["2"],', '"Over 2.5": 1 - m_poisson["u25"], "Under 2.5": m_poisson["u25"],', '"GG": m_poisson["gg"], "NG": 1 - m_poisson["gg"]', '}', 'best_mkt = max(mercati, key=mercati.get)', 'poisson_prob = mercati[best_mkt]', 'elo_prob, elo_disponibile = poisson_prob, True', 'try:', 'elo_p = predict_elo_probs(h, a, league)', 'if best_mkt == f"Vittoria {h}":', 'elo_prob = elo_p["1"]', 'elif best_mkt == f"Vittoria {a}":', 'elo_prob = elo_p["2"]', 'elif best_mkt == "Pareggio":', 'elo_prob = elo_p["X"]', 'except Exception as e:', 'elo_disponibile = False', 'logging.warning(f"Elo non disponibile per {h} vs {a} ({league}): {e}")', 'if best_mkt in ["Over 2.5", "Under 2.5", "GG", "NG"] or not elo_disponibile:', 'confidence = poisson_prob', 'min_conf = 0.60', 'else:', 'confidence = ELO_ENSEMBLE_W * poisson_prob + (1 - ELO_ENSEMBLE_W) * elo_prob', 'min_conf = 0.55', 'if confidence >= min_conf and abs(poisson_prob - elo_prob) < 0.25:']
